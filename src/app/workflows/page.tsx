import type { Metadata } from "next";
import { ComingSoon } from "@/components/layout/coming-soon";

export const metadata: Metadata = {
  title: "워크플로우",
};

export default function WorkflowsPage() {
  return <ComingSoon title="워크플로우" />;
}
