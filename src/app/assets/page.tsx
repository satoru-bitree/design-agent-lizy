import type { Metadata } from "next";
import { ComingSoon } from "@/components/layout/coming-soon";

export const metadata: Metadata = {
  title: "에셋 · Agentic Creative",
};

export default function AssetsPage() {
  return <ComingSoon title="에셋" />;
}
